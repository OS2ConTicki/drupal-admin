<?php

/**
 * @file
 * Module file for conference_content.
 */

use Drupal\Core\Form\FormStateInterface;

/**
 * Implements hook_form_alter().
 */
function conference_content_form_alter(
  &$form,
  FormStateInterface $form_state,
  $form_id
) {
  \Drupal::service('conference_content.form_helper')->alter($form, $form_state, $form_id);
}

/**
 * Implements hook_theme().
 */
function conference_content_theme() {
  return [
    'conference_content_conference_info' => [
      'variables' => [
        // The conference.
        'conference' => NULL,
      ],
    ],
    'conference_content_conference_entity_list' => [
      'variables' => [
        // The conference.
        'conference' => NULL,
        // The entity type.
        'type' => NULL,
        // The entities.
        'entities' => NULL,
      ],
    ],
  ];
}
